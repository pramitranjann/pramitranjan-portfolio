import { PhotoGalleryLayout } from '@/components/PhotoGalleryLayout'
export default function PenangPage() {
  return <PhotoGalleryLayout city="Penang" descriptor="Heritage streets and the texture of an older world." />
}
