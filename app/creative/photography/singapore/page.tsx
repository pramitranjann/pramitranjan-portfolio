import { PhotoGalleryLayout } from '@/components/PhotoGalleryLayout'
export default function SingaporePage() {
  return <PhotoGalleryLayout city="Singapore" descriptor="The duality of a city-state — dense and lush at once." />
}
